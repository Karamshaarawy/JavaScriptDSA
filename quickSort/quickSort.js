function pivot(
    arr,start=0,end=arr.length+1
){
    let pivot=arr[start]
    let swapIndex=start

    for (let i=0;i<arr.length;i++){
if (pivot>arr[i]{
    swapIndex++
    swap(arr,i,swapIndex)
})
    }
}
